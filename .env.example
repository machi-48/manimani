# Turso（未設定ならローカルの file:manimani.db を使う）
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# ログイン。両方揃うと認証が有効になる。
# ローカル開発では未設定のままで素通しできる。本番で未設定だと 500 で止まる。
APP_PASSWORD=
# openssl rand -hex 32 などで生成した長いランダム文字列
APP_SECRET=
